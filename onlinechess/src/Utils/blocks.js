import { BOARDSIZE } from "./configs";

export const getColor = (i, j) => ((i + j) % 2 == 0 ? "#ccc" : "#777");

const getKingPosition = (board, color) => {
  for (let i = 0; i < BOARDSIZE; i++) {
    for (let j = 0; j < BOARDSIZE; j++){

    }
  }
}
